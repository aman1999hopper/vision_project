import React from "react";

// Define a functional component for the "What we offer span" greeting

export default function Text({value}){
    return (
        <span className="font-semibold text-center">{value}</span>
    );

}